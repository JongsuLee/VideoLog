<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.member.MemberMapper">
 
   <insert id="userAdd" parameterType="User">
   	  insert into member (userId, passwd, userName, gender, age)
   	  values (#{userId},#{passwd},#{userName},#{gender},#{gender})
   </insert>
   
</mapper>















